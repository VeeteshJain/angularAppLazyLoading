define([
	'app',
	'angular',
	], function(app,angular) {
	'use strict';

  	/* Directives */
	app.directive('headerDir', ['', function() {

		function link(scope,element,attrs){

		}

		return {
			link : link,
			scope : {},
		}
	}]);
});
